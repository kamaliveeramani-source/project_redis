# project_redis
pip install redis
python -m venv venv
.venv/scripts/activate
main py file edit
notepad main.py
pip freeze requirements
cat requirements.txt
pip install -r requirements.txt
git add push 
